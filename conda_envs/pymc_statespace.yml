name: pymc-statespace
channels:
- conda-forge
- numba
dependencies:
  # Base dependencies
  - pymc
  - pytensor
  - numba::numba
  - numpy
  - pandas
  - xarray
  - matplotlib
  - arviz
  # Testing dependencies
  - pre-commit
  - pytest-cov>=2.5
  - pytest>=3.0
