name: pymc-statespace
channels:
- conda-forge
dependencies:
  # Base dependencies
  - pymc
  - pytensor
  - numba::numba>=0.57
  - numpy
  - pandas
  - xarray
  - matplotlib
  - arviz
  - statsmodels
  # Testing dependencies
  - pre-commit
  - pytest-cov>=2.5
  - pytest>=3.0
